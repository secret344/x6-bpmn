/**
 * 核心数据模型层 — 模块入口
 */

export {
  getFieldDefaultValue,
  normalizeFieldValue,
  validateFieldValue,
  serializeFieldValue,
  deserializeFieldValue,
  getFieldsForCategory,
  getFieldsForShape,
  buildDefaultData,
  validateFields,
} from './fields'
